/*
 * main.cpp
 *
 *  Created on: 27 cze 2019
 *      Author: Denis
 */
#include <cstdlib>	//all three needed for usage of srand
#include <iostream>
#include <fstream>	//usage of extern files
#include <ctime>

#include "locale.h" //for setlocale
#include "CGraph.h"
#include "CList.h"

using namespace std;

void PrintList()  // object is unchanged
{
	slistelem* temp = ;
	while (temp != 0) { // detect end of slist
		testFile << temp -> data << " -> ";
		temp = temp -> next;
	}
	cout << "\n###" << endl;
}

int main()
{
	ofstream testFile;
	setlocale(LC_ALL, "polish");
	srand(time(0));

cout<<"unut"<<endl;

CList obj1;


CGraph objMain(obj1);

testFile.("test.txt");

slistelem* temp = obj1.first();
	while (temp != 0) { // detect end of slist
		testFile << temp -> data << " -> ";
		temp = temp -> next;
	}
	cout << "\n###" << endl;

}
